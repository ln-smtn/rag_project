# Проект: Оценка качества векторного поиска для RAG-систем

## 📋 Содержание

1. [Введение](#введение)
2. [Теоретическая основа](#теоретическая-основа)
3. [Описание датасета](#описание-датасета)
4. [Архитектура решения](#архитектура-решения)
5. [Реализация](#реализация)
6. [Результаты](#результаты)
7. [Использование](#использование)

---

## Введение

Данный проект демонстрирует реализацию системы оценки качества векторного поиска для задач Information Retrieval и Retrieval Augmented Generation (RAG). Проект использует датасет новостей на русском языке и оценивает эффективность различных моделей эмбеддингов для поиска релевантных документов.

### Основные задачи проекта:

- Загрузка и подготовка датасета для retrieval-задач
- Создание векторных представлений текстов с помощью моделей эмбеддингов
- Построение векторной базы данных на основе FAISS
- Оценка качества поиска с использованием метрик Top-K accuracy
- Сравнение различных моделей эмбеддингов

---

## Теоретическая основа

### Что такое эмбеддинги?

**Эмбеддинги (embeddings)** — это числовые представления объектов данных (в нашем случае — текстов), которые кодируют их семантическое значение. Эмбеддинги представляют собой векторы в многомерном пространстве, где:

- **Похожие объекты находятся близко друг к другу** в векторном пространстве
- **Расстояние между векторами** отражает семантическую близость текстов
- **Чем ближе два вектора, тем больше они похожи по смыслу**

#### Пример аналогии:

Как цвета в системе RGB представлены тремя числами (красный, зеленый, синий), так и тексты в системе эмбеддингов представлены векторами чисел (обычно 384, 512, 768 или более измерений).

```
Текст 1: "Машинное обучение" → [0.23, -0.45, 0.67, ..., 0.12]
Текст 2: "Искусственный интеллект" → [0.25, -0.43, 0.65, ..., 0.14]
Текст 3: "Рецепт борща" → [-0.12, 0.34, -0.56, ..., -0.23]
```

В этом примере первые два текста будут близки в векторном пространстве (похожие значения), а третий — далек от них.

### Векторные базы данных

**Векторная база данных (Vector Database)** — это специализированная СУБД, предназначенная для эффективного поиска близких векторов в многомерных пространствах.

#### Основные принципы:

1. **Проблема полного перебора**: При поиске похожих векторов в базе из миллионов документов полный перебор всех векторов неэффективен.

2. **Графовые структуры**: Для ускорения поиска используются графовые структуры, связывающие ближайшие векторы.

3. **Многослойные графы (HNSW)**: Популярный алгоритм **Hierarchical Navigable Small World (HNSW)**:
   - Граф "огрубляется" на несколько слоев
   - Верхние слои содержат меньше вершин и используются для быстрого приближения
   - Нижние слои содержат все векторы и используются для точного поиска
   - Внутри слоя происходит перебор только ближайших соседей

#### Преимущества векторных БД:

- **Быстрый поиск**: Поиск среди миллионов векторов за миллисекунды
- **Масштабируемость**: Эффективная работа с большими объемами данных
- **Точность**: Сохранение семантических связей между документами

### Retrieval Augmented Generation (RAG)

**RAG** — это архитектурный подход, который сочетает большие языковые модели (LLMs) с внешними источниками информации для улучшения качества и актуальности генерируемых ответов.

#### Как работает RAG:

1. **Retrieval (Поиск)**: При получении запроса система ищет релевантные документы в векторной базе данных
2. **Augmentation (Обогащение)**: Найденные документы добавляются к контексту запроса
3. **Generation (Генерация)**: LLM генерирует ответ на основе обогащенного контекста

#### Преимущества RAG:

- **Актуальность**: Доступ к самой свежей информации
- **Точность**: Ответы основаны на реальных документах
- **Прозрачность**: Можно отследить источники информации
- **Преодоление ограничений**: LLM не ограничена только своими обучающими данными

---

## Описание датасета

### Датасет: ai-forever/ria-news-retrieval

Проект использует датасет **RIA News Retrieval** от AI Forever, который является частью бенчмарка [MTEB (Massive Text Embedding Benchmark)](https://huggingface.co/blog/mteb).

**Ссылка на датасет**: https://huggingface.co/datasets/ai-forever/ria-news-retrieval

#### Структура датасета:

Датасет состоит из **трех основных компонентов**:

1. **Queries (Запросы)** — 10,000 запросов на русском языке
   - Колонки: `_id`, `text`
   - Пример: `"сми: планируется создание в рф несетевых магазинов возле жилых домов"`

2. **Corpus (Корпус документов)** — 704,344 новостных статей
   - Колонки: `_id`, `title`, `text`
   - Каждый документ содержит заголовок и текст новости

3. **Test/Default (Эталонные соответствия)** — 10,000 соответствий между запросами и документами
   - Колонки: `query-id`, `corpus-id`, `score`
   - Определяет, какой документ является правильным ответом на каждый запрос
   - Пример: `{'query-id': '0', 'corpus-id': '670487', 'score': 1}`

#### Особенности датасета:

- **Язык**: Русский
- **Домен**: Новостные статьи (RIA News)
- **Размер корпуса**: 704,344 документов
- **Количество запросов**: 10,000
- **Тип задачи**: Information Retrieval (поиск релевантных документов)

---

## Архитектура решения

### Общая схема работы:

```
┌─────────────┐
│   Queries   │  (10,000 запросов)
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Embedding Model │  (Преобразование в векторы)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Vector Search  │  (Поиск в векторной БД)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│   Top-K Docs    │  (K наиболее похожих документов)
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│   Evaluation    │  (Сравнение с ground truth)
└─────────────────┘
```

### Компоненты системы:

1. **Модель эмбеддингов**: Преобразует тексты в векторные представления
2. **Векторная база данных (FAISS)**: Хранит и индексирует векторы документов
3. **Система оценки**: Сравнивает результаты поиска с эталонными данными

---

## Реализация

### Структура ноутбука `rag_project.ipynb`

Проект реализован в Jupyter Notebook и состоит из следующих этапов:

#### 1. Загрузка датасета

```python
# Загрузка основного датасета
dataset = load_dataset("ai-forever/ria-news-retrieval")

# Загрузка отдельных компонентов
queries = load_dataset("ai-forever/ria-news-retrieval", name="queries", split="queries")
corpus = load_dataset("ai-forever/ria-news-retrieval", name="corpus", split="corpus")
test = dataset["test"]
```

**Особенности реализации**:
- Автоматическое определение структуры датасета
- Проверка наличия текстовых колонок в corpus
- Создание маппингов между ID и индексами

#### 2. Выбор модели эмбеддингов

**Используемая модель**: `sentence-transformers/distiluse-base-multilingual-cased-v1`

- **Тип**: Multilingual Universal Sentence Encoder (DistilUSE)
- **Размерность эмбеддингов**: 512
- **Языки**: Поддержка множества языков, включая русский
- **Архитектура**: Основана на Transformer, оптимизирована для скорости

**Альтернативные модели** (для экспериментов):
- `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- `sentence-transformers/LaBSE`

#### 3. Создание векторной базы данных

**Технология**: FAISS (Facebook AI Similarity Search)

```python
# Создание эмбеддингов для корпуса
corpus_embeddings = embedding_model.encode(
    corpus_texts,
    show_progress_bar=True,
    batch_size=32,
    convert_to_numpy=True
)

# Нормализация векторов для косинусного сходства
corpus_embeddings = corpus_embeddings / np.linalg.norm(corpus_embeddings, axis=1, keepdims=True)

# Создание FAISS индекса
index = faiss.IndexFlatIP(dimension)  # Inner Product для нормализованных векторов
index.add(corpus_embeddings.astype('float32'))
```

**Особенности**:
- Использование `IndexFlatIP` (Inner Product) для нормализованных векторов = косинусное сходство
- Нормализация векторов обеспечивает корректное вычисление косинусного расстояния
- Индексация всех 704,344 документов

#### 4. Подготовка данных для оценки

```python
# Создание словаря запросов
query_dict = {query_id: query_text for ...}

# Создание ground truth (правильные соответствия)
ground_truth = {query_id: corpus_id for ...}

# Маппинги для быстрого поиска
corpus_id_to_index = {corpus_id: index for ...}
```

#### 5. Функция оценки качества: `evaluate_retrieval()`

**Параметры функции**:
- `query_dict`: Словарь запросов (query_id → текст)
- `ground_truth`: Правильные соответствия (query_id → corpus_id)
- `embedding_model`: Модель для создания эмбеддингов
- `index`: FAISS индекс
- `k_values`: Список значений K для оценки (например, [1, 3, 5, 10])

**Алгоритм работы**:

1. **Создание эмбеддингов запросов**: Все запросы преобразуются в векторы
2. **Поиск для каждого запроса**:
   - Выполняется поиск Top-K наиболее похожих документов
   - Получаются индексы и расстояния найденных документов
3. **Проверка правильности**:
   - Для каждого K проверяется, есть ли правильный `corpus_id` в Top-K результатах
   - Подсчитывается количество правильных ответов
4. **Логирование ошибок**: Запросы, где правильный ответ не найден в Top-3

**Метрики оценки**:
- **Top-1 Accuracy**: Процент запросов, где правильный ответ на первом месте
- **Top-3 Accuracy**: Процент запросов, где правильный ответ в первых трех результатах
- **Top-5 Accuracy**: Процент запросов, где правильный ответ в первых пяти результатах
- **Top-10 Accuracy**: Процент запросов, где правильный ответ в первых десяти результатах

#### 6. Вывод результатов

Функция возвращает словарь со статистикой:
```python
{
    'total_queries': 10000,
    'processed_queries': 10000,
    'skipped_queries': 0,
    'correct_by_k': {1: 3141, 3: 4515, 5: 5091, 10: 5811},
    'accuracy_by_k': {1: 0.3141, 3: 0.4515, 5: 0.5091, 10: 0.5811},
    'errors': [...]
}
```

#### 7. Сравнение моделей: `compare_models()`

Функция позволяет сравнить несколько моделей эмбеддингов:

```python
comparison_results = compare_models(
    model_names=[
        "sentence-transformers/distiluse-base-multilingual-cased-v1",
        "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2",
        "sentence-transformers/LaBSE"
    ],
    ...
)
```

**Результат**: Сравнительная таблица с метриками для каждой модели.

---

## Результаты

### Результаты оценки для модели `distiluse-base-multilingual-cased-v1`

На основе выполненной оценки получены следующие результаты:

| Метрика | Значение | Описание |
|---------|----------|----------|
| **Top-1 Accuracy** | 31.41% | Правильный ответ на первом месте в 31.41% случаев |
| **Top-3 Accuracy** | 45.15% | Правильный ответ в топ-3 в 45.15% случаев |
| **Top-5 Accuracy** | 50.91% | Правильный ответ в топ-5 в 50.91% случаев |
| **Top-10 Accuracy** | 58.11% | Правильный ответ в топ-10 в 58.11% случаев |

#### Статистика:

- **Всего запросов**: 10,000
- **Обработано запросов**: 10,000
- **Размер корпуса**: 704,344 документов
- **Количество ошибок** (не найдено в Top-3): 5,485

#### Анализ результатов:

1. **Top-1 Accuracy (31.41%)**: Модель находит правильный документ на первом месте примерно в трети случаев. Это приемлемый результат для такой большой базы документов (704K).

2. **Top-3 Accuracy (45.15%)**: Почти половина запросов имеет правильный ответ в первых трех результатах, что делает систему пригодной для RAG-приложений, где можно использовать несколько кандидатов.

3. **Рост точности с увеличением K**: По мере увеличения количества возвращаемых результатов точность растет, что ожидаемо.

4. **Возможности улучшения**:
   - Использование более мощных моделей эмбеддингов
   - Применение ре-ранкинга (re-ranking) результатов
   - Использование более сложных индексов FAISS (например, HNSW)

### Сохранение результатов

Результаты сохраняются в файл `results.json`:

```json
{
  "model": "sentence-transformers/distiluse-base-multilingual-cased-v1",
  "vector_store": "FAISS (IndexFlatIP)",
  "corpus_size": 704344,
  "total_queries": 10000,
  "processed_queries": 10000,
  "accuracy": {
    "1": 0.3141,
    "3": 0.4515,
    "5": 0.5091,
    "10": 0.5811
  },
  "num_errors": 5485
}
```

---

*Сметанина Елена**: Проект выполнен в рамках изучения векторных баз данных и RAG-систем.

**Дата**: 2025
